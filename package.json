{
  "name": "charaka",
  "devDependencies": {
    "bulma": "^1.0.3",
    "sass": "^1.86.3"
  },
  "scripts": {
    "css-build": "sass --load-path=node_modules --no-source-map --style=compressed scss/styles.scss static/css/styles.css",
    "css-watch": "npm run css-build -- --watch",
    "start": "npm run css-watch"
  }
}
